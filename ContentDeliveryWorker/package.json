{
  "name": "vlviewer-content-cdn-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "dev": "wrangler dev --local --persist-to .wrangler/state",
    "dev:preview": "wrangler dev --local --persist-to .wrangler/preview-state",
    "seed:local": "node scripts/seed-local.mjs",
    "seed:preview": "node scripts/seed-local.mjs --persist-to .wrangler/preview-state --reset",
    "smoke:local": "node scripts/smoke-local.mjs"
  },
  "devDependencies": {
    "wrangler": "4.129.0"
  }
}
